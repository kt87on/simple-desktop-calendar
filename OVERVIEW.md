# 桌面日历 2.0 · 视觉升级交付说明

## 一、目标与定位
1.0（根目录）已锁定全部**功能与要素**，2.0 在其基础上做**纯视觉升级**——更美、更易用，功能规则原样保留。
由 UI Designer 主导，调用 impeccable 专业设计方法论。

## 二、目录结构（基础版 + 2.0）
```
D:\AI\日历\
├── app.js / template.html / lunar.min.js / build.js / electron-main.js / preload.js / package.json   ← 1.0 基础版（功能基线）
├── baseline_v1\          ← 1.0 文件存档副本（已固化，作为功能基准）
└── 2.0\                  ← 视觉 2.0 工作区（本次交付）
    ├── template.html     ← 全新视觉模板（纸与光）
    ├── app.js            ← 复用 1.0 逻辑 + color-mix 主题 + 淡蓝灯效
    ├── build.js          ← 指向 2.0 路径
    ├── electron-main.js  ← 窗口 490×600 贴合 430×540
    ├── package.json      ← 同 1.0（dev/build 脚本）
    ├── lunar.min.js      ← 农历库（共享）
    ├── DESIGN_SPEC.md    ← 设计系统方案
    └── calendar.html     ← 构建产物（335,631 字节）
```

## 三、2.0 设计要点（相比 1.0 的美学提升）
| 维度 | 1.0 | 2.0 |
|---|---|---|
| 底色 | 纯白带灰 | 暖纸白 `rgba(252,251,249,0.82)`（宣纸感） |
| 文字 | 近黑 `#14161d` | 墨色 `#1f2430`（更柔和） |
| 主蓝 | AI 标准蓝 `#4d8bff` | 雅蓝/群青 `#3b6fd4`（更东方、沉稳） |
| 节假日红 | 亮红 `#ff3b30` | 朱砂红 `#d6453f`（更中国） |
| 主题派生 | 手工拆 `--accent-deep/--accent-rgb` | `color-mix` 自动派生，跟随系统更统一 |
| 周起始 | 文字滑块 | 真正 toggle（轨道+滑块+一/日标签，平滑过渡） |
| 时钟 | 36px/600 | 40px/600，更舒展 |
| 深度 | 圆角+通用阴影 | 单层柔和阴影 + 内高光（去俗套） |
| 灯效 | 淡蓝 `rgba(120,170,255,...)` | 更柔淡蓝 `rgba(120,160,240,...)` |
| 形态 | 410×500 | 430×540（更舒展仍竖长方形） |

## 四、保留的 1.0 功能规则
周一/周日起始切换 · 公历主数字 + 农历/节气/节日副字 · 节假日淡红+休 · 补班蓝+班 ·长假期仅节首日显示节日名 · 跨月透明红 · 节日白名单（24 节气+法定+中国传统+国外主流民俗，过滤国际/全国/行业类）· 今日=选中（浅蓝透明）· 灯效 · 主题默认/跟随系统 · 放大按钮 · 非本月更淡 · 无点选闪烁 · 双击放大已移除

## 五、运行方式（在 2.0 目录）
```bash
cd D:\AI\日历\2.0
node build.js              # 重建 calendar.html（无需安装依赖）
npm install                # 首次需装 Electron（仅打包/运行桌面应用时）
npm run dev                # Electron 弹出桌面日历（可读取 Win 强调色）
npm run build              # 打包 .exe（NSIS 安装版 + portable 免安装版）
```

## 六、后续可选增强
- 浅色/深色双主题切换（不仅跟随系统）
- 今日格加一个柔和"墨点"标识，与选中在视觉上再做细微区分
- 周起始开关标签本地化/图标化
- 灯效强度随系统深浅主题自适应
