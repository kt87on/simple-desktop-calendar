# 简洁桌面日历 · 软件介绍与使用说明

> 受够了 Windows 原生日历，也实在受不了满屏广告、账号、会员的第三方日历？这款极简桌面日历，**打开就是一张日历**，只做三件事：**看日历、看放假、看上班**，外加一个真正有用的「特别关注」到期提醒。

---

## 一、软件介绍

**简洁桌面日历**是一款 Windows 桌面日历软件。它常驻在任务栏右端显示实时时钟，点击即可唤出一张无边框的漂亮日历，随点随用、点外即隐，不打扰你的桌面。

### 核心特点

- **极简**：无广告、无账号、无云同步、无待办/天气等多余功能。
- **任务栏实时时钟**：任务栏右端常驻「时:分:秒 + 星期/日期」，像原生时钟一样长在任务栏上，不占任务栏按钮。
- **放假/上班一目了然**：法定节假日标红「休」、调休补班标蓝「班」。
- **农历/节日/节气**：每天副字显示农历、节日或 24 节气。
- **区间算天数**：点两个日期，自动算出「共计 N 天」，支持跨月。
- **特别关注提醒**：重要日期标金黄「注」，到期当天自动弹窗提醒。
- **桌面插件**：把日历板块作为一块桌面工具摆在桌面上，可拖动、可锁定。
- **皮肤系统**：皮肤窗主视图有**三个入口**——「**原生皮肤**」「**浮窗样式**」与「**自选图片**」。点「原生皮肤」会动画展开 **6 套风格**（默认 / 极简 / 玻璃拟态 / 新拟态 / 科技感 / 温润拟物，每套都带迷你预览），**选中即让日历 / 浮窗 / 桌面插件 / 放大窗口四个界面统一切成该风格**；点「浮窗样式」会动画展开 **6 款造型**——**圆角卡片（默认）/ 像素方屏 / 虹彩流光 / 极简圆环 / 翻页时牌 / 绘本台钟**，选中即换浮窗外观（窗口尺寸随造型自适应，方屏 / 圆环 / 台钟会离开任务栏变成桌面挂件）；点「自选图片」会打开一个**独立新窗口**，可为单个界面（日历 / 浮窗 / 桌面插件 / 放大窗口）分别设置图片、文字明暗、UI 清晰度、透明度与「跟随主界面」，并可一键「清空」图片回到原生皮肤。
- **浮窗样式（v3.3.0 新增）**：默认造型「圆角卡片」就是原来那条**贴任务栏的小方框**；除「虹彩流光」外的其余四款会变成带投影余量的桌面挂件。浮窗在「跟随主界面」时统一为圆角卡片，**选任一其他造型即自动转为独立**。在这五款造型下**导入自选图片会自动切回「圆角卡片」并显示该图**（它们自带材质、不吃图片皮肤），**单纯换造型不会清掉已选图片**——图片留在配置里，切回「圆角卡片」即恢复显示；「清空」按钮只清图片、**不改造型**。其中**「虹彩流光」**现在也能像「圆角卡片」一样**贴住任务栏上沿与屏幕右沿**（右、下内边距归零，默认落点与「圆角卡片」同一条公式），不再是悬浮的桌面挂件。
- **原生风格默认「四处统一」**：选一套原生风格后，日历 / 浮窗 / 桌面插件 / 放大窗口**统一切成同一风格**（浮窗即便此前单独设过图片也会被覆盖）；若在「自选图片」窗口里单独调整过某个界面，该界面会自动转为独立、不再跟随（可用「跟随主界面」开关改回）。
- **文字明暗 & UI 清晰度**：文字明暗可在自动 / 深字 / 浅字间切换（只改文字、不改整窗明暗）；UI 清晰度可按背景复杂度自动推导，也可手动调保护强度。
- **自定义图片背景**：可为任一界面设置本地图片背景（支持 PNG / JPG / **GIF 动图** / WebP / **MP4 动图**），可与任意风格叠加；拖动取景、滚轮缩放，文字明暗按图片亮度自动判定（MP4 无法采样首帧亮度，默认按浅色兜底，可在设置里手动改）。
- **放大模式**：点放大镜「+」近全屏放大，字号同步变大，放大后可拖动窗口（不越屏幕）。

### 基本信息

| 项目 | 内容 |
|---|---|
| 软件名称 | 简洁桌面日历 |
| 当前版本 | v3.4.0 |
| 安装包大小 | 约 78 MB |
| 安装后占用 | 约 260 MB（含运行组件） |
| 运行平台 | Windows 10 / Windows 11（无需管理员权限） |
| 许可协议 | [MIT](LICENSE)，免费使用 |
| 官方网站 | https://github.com/kt87on/simple-desktop-calendar |

### 界面速览

| 日历主体（浅色） | 日历主体（深色） |
|---|---|
| ![日历-浅色](preview/calendar-electron-light.png) | ![日历-深色](preview/calendar-electron-dark.png) |

---

## 二、安装与启动

### 系统要求

- Windows 10 或 Windows 11
- 无需管理员权限（默认安装到当前用户目录）

### 安装步骤

1. 从 [GitHub Releases](https://github.com/kt87on/simple-desktop-calendar/releases) 下载 `SimpleCalendar-Setup-3.4.0.exe`。
2. 双击运行，按向导「下一步 → 完成」。
3. 安装完成后可勾选「立即运行」与「查看说明文档」。

> 默认会创建**桌面快捷方式**和**开始菜单快捷方式**，并设置为**开机自启**。

### 卸载

控制面板 → 程序和功能 → 简洁桌面日历 → 卸载；或点开始菜单里的「卸载简洁桌面日历」。

---

## 三、功能说明

### 3.1 任务栏挂件条（浮动时钟）

任务栏右端常驻一个实时时钟（上行「时:分:秒」、下行「星期 月/日」）。用法：

- **点一下**：打开 / 关闭日历主窗口。
- **按住拖动**：移到屏幕任意位置（拖不出屏幕、也压不到任务栏），落点会被记住。
- **右键**：弹出功能菜单（最近节日 / 特别关注 / 显示·隐藏日历 / 设置 / 退出软件）。

它有两种形态（设置里切换）：

- **浮窗**（默认）：可拖动的小方框，始终可见。
- **托盘图标**：收起插件，只在系统托盘留一个静态图标，左键点它切回插件。

### 3.2 日历主窗口

- 「‹ ›」箭头翻上 / 下月；年、月下拉框直接跳转到任意年/月；「今」一键回到当前月。
- 每格日期下方显示农历、节日或 24 节气。
- 法定节假日红底标「休」，调休补班标「班」。
- 点第一个日期 + 第二个日期，中间连成色带并显示「共计 N 天」，支持跨月。
- 点放大镜「+」放大，可拖动窗口，拖四角可等比缩放。
- 主题 / 明暗在「设置 → 皮肤设置」里调整（v3 起主窗不再有 ☾/☀ 按钮）：选「原生皮肤」统一切风格，或进「自选图片」独立窗口单独调节文字明暗等。

### 3.3 特别关注 / 提醒

1. 右键点击某个日期 → 输入备忘（**15 字以内**）→ 确认。
2. 该格变**金黄底 + 「注」角标**。
3. 到日期当天，自动弹出提醒窗口：
   - 「知道了」：当天不再弹；
   - 「稍后提醒」：30 分钟后再弹。

- 点工具栏的书图标，打开**关注列表窗口**批量查看 / 一键取消（该窗口可拖到屏幕任意位置）。
- 关注数量不限，每条 15 字以内。

### 3.4 桌面插件

在「设置」中开启「桌面插件」，把**日历板块**（不含顶部时间条）作为一块桌面工具：

- **拖动**：按住移到任意位置（屏幕边框与任务栏为界）。
- **锁定**：点锁定键固定位置，不再被误拖，其余功能照常。

### 3.5 设置

右键菜单 →「设置」打开设置弹窗（可拖动窗口），按 4 个分区集中管理：

- **功能区**：皮肤设置入口（打开独立皮肤设置窗口）、开机自启、检查节假日更新。
- **桌面插件区**：桌面插件开关、桌面插件锁定。
- **浮窗区**：挂件条总开关、显示形态（浮窗 / 托盘图标）、插件总在最前。
- **日历窗口区**：日历窗口置顶。
- **皮肤设置**：皮肤窗主视图有**三个入口**。点「**原生皮肤**」动画展开 **6 套风格**（默认 / 极简 / 玻璃拟态 / 新拟态 / 科技感 / 温润拟物，每套带迷你预览），**选中即让日历 / 浮窗 / 桌面插件 / 放大窗口四个界面统一切成该风格**。点「**浮窗样式**」动画展开 **6 款造型**（圆角卡片 / 像素方屏 / 虹彩流光 / 极简圆环 / 翻页时牌 / 绘本台钟），选中即换浮窗外观，**默认圆角卡片就是原来的任务栏小条**，除「虹彩流光」外的其余四款会变成桌面挂件、窗口尺寸随之变化。点「**自选图片**」会打开一个**独立新窗口**，在其中选择界面（日历 / 浮窗 / 桌面插件 / 放大窗口），可为该界面单独设置**图片**（拖拽导入、取景、图片不透明度）、**文字明暗**（自动 / 深字 / 浅字，只改文字不改整窗）、**UI 清晰度**（自动按背景复杂度推导 / 手动调保护强度）、**界面透明度**与「**跟随主界面**」开关。
  - **清空图片（v3.3.0 新增）**：在「自选图片」窗口的「图片取景」标题右侧有「**清空**」按钮，一键清掉当前界面的自选图片、**回到原生皮肤**（造型不变，盘上图片文件不会删除）；本界面没有图片时按钮为灰色不可点。
  - **跟随主界面**：打开时与主界面**完全一致**；只要单独调整取景 / 文字 / 清晰度等任一项即**自动转为独立，并回到原生皮肤**。
  - **浮窗大小（v3.3.0 新增）**：在「自选图片」窗口选择「浮窗界面」时，下方出现「浮窗大小 → 缩放比例」拉杆，可把整个浮窗**等比放大 / 缩小**（**60% ~ 160%，步进 5%，默认 100%**）。它调的是几何大小，与「跟随主界面」开关**相互独立** —— 拖动它不会让浮窗脱离跟随。
- **四处默认统一**：选一套原生风格后，日历 / 浮窗 / 桌面插件 / 放大窗口**统一切成同一风格**（浮窗即便此前单独设过图片也会被覆盖）；若在「自选图片」窗口里单独调整过某个界面（例如只给浮窗换张图），该界面会自动转为独立、不再跟随，可用「跟随主界面」开关改回。
- **数据**：手动检查节假日更新。

### 3.6 节假日自动更新

软件联网检查最新放假安排，有新数据自动弹窗确认；更新成功显示明细、失败提示原因。可在设置中随时手动检查。

---

## 四、版本更新信息

| 版本 | 日期 | 说明 |
|---|---|---|
| **v3.4.0** | 2026-09 | 自定义图片背景新增 **MP4 动图**支持（独立 `<video>` 层，静音循环自动播放；取景 / 缩放 / 不透明度 / 文字明暗与图片完全一致；导入即复制进数据目录）。支持格式扩展为 **PNG / JPG / GIF 动图 / WebP / MP4 动图**。已知限制：**MP4 音轨一律不播放（静音）**；**无法采样视频首帧亮度，文字明暗默认按浅色兜底、可在设置里手动改**。 |
| **v3.3.0** | 2026-09 | 新增「**浮窗样式**」入口：浮窗可选 **6 款造型**——**圆角卡片（默认）/ 像素方屏 / 虹彩流光 / 极简圆环 / 翻页时牌 / 绘本台钟**，窗口尺寸随造型自适应（除「虹彩流光」外的非圆角造型变为带投影余量的桌面挂件）；「自选图片」窗口新增「**清空**」按钮，一键清掉图片、回到原生皮肤（盘上文件不删），并在选中「浮窗界面」时新增「**浮窗大小**」等比缩放拉杆（**60% ~ 160%、步进 5%**，与跟随开关相互独立）；修复「浮窗不跟随主界面时图片框仍显示主界面图片」的问题（取消跟随即回到空白态，图片不再被继承）；跟随开关语义明确为「关掉跟随回到原生皮肤」。观感修复：**窗口圆角外不再露出一圈不透明的直角底色**（顺带让卡片毛玻璃背景第一次真正生效）、**极简圆环与像素方屏造型外围的方形暗框已去除**。 |
| **v3.2.0** | 2026-09 | 命名统一：界面名词统一为「**浮窗**」；皮肤窗改版——主视图只剩「**原生皮肤**」「**自选图片**」两个入口。点「原生皮肤」动画展开 **6 套风格**（每套带迷你预览），**选中即让日历 / 浮窗 / 桌面插件 / 放大窗口四个界面统一切成该风格**；点「自选图片」改为打开**独立新窗口**，可按界面单独设置图片 / 文字明暗 / UI 清晰度 / 透明度 / 跟随主界面（**移除「背景来源」与「明暗」两项**）。 |
| **v3.1.0** | 2026-09 | 皮肤窗精简为 **6 行原生风格列表**（原生风格固定、点一行即应用），图片导入与清晰度/文字深浅等细调移入「**自选图片**」浮层；六套原生风格**视觉重做**（科技感→赛博朋克霓虹、玻璃拟态→磨砂玻璃+光泽、新拟态→真正的双向挤出与内凹控件）；**原生风格默认「四处统一」**（浮窗纳入跟随名单，手调后自动转独立）；特别关注：关注列表新增**「定时」独立列**（日期/内容/定时/删除）与**窗口内直接录入**（无需回日历逐个点击）；单元格右键弹窗在内容框下方新增**定时提示**；修复升级后浮窗自定义配置被静默忽略、手动调整被静默忽略、提醒弹窗被置顶主窗遮挡、深色档上下月日期几乎不可见。 |
| **v3.0.0** | 2026-09 | 皮肤系统三轴重构：每个界面（日历 / 放大 / 桌面插件 / 浮窗）独立设置「风格（6 种材质：默认 / 极简 / 玻璃拟态 / 新拟态 / 科技感 / 温润拟物）× 背景来源（原生 / 自选图片，可叠加）× 明暗（自动 / 浅色 / 深色 / 跟随系统）」；新增「文字明暗」轴（只改文字、不再整窗变色）；「UI 清晰度」对全部风格通用；移除「黑夜」独立主题与「纯色」皮肤，「跟随系统」并入明暗轴。 |
| **v2.4.4** | 2026-09 | 修复手机竖拍照片（JPEG EXIF 方向）导入后被上下压扁；新增「UI 清晰度」调节（自选纯色/图片皮肤）：文字光晕 + 局部背景保护 + 状态元素晕环三层可读性，自动档按背景复杂度智能推导，也可手动拖动滑杆微调。 |
| **v2.4.0** | 2026-09 | 皮肤系统重构：四个界面可独立设置浅色/深色/跟随系统/纯色/图片皮肤；新增独立皮肤设置窗口；支持自定义图片背景（PNG/JPG/GIF 动图/WebP），文字自动适配明暗；修复浮窗纯色模式点击闪黑、方框边与两层堆叠，修复 GIF 动图导入识别。 |
| **v2.3.2** | 2026-09 | 放大/缩小按钮重做为放大镜图标；放大模式窗口可拖动且不越屏幕；工具栏改灰并修复漏白边；年月/时间栏/农历字号加大加粗。 |
| **v2.3.1** | 2026-09 | 白日模式工具栏对比度提升；下月日期单元格清晰度优化。 |
| **v2.3.0** | 2026-09 | 设置窗口支持拖动；解除特别关注数量限制；复查优化 Windows 11 兼容；重做使用说明并修复安装后说明未弹出。 |
| v2.2.0 | 2026-09 | 新增桌面插件与设置弹窗；三档窗口透明度；放大等比缩放；点击开关日历；插件显示秒+星期；打开回当前月。 |
| v2.1.0 | 2026-09 | 节假日年度联网更新；修复日历跨天。 |
| v2.0.0 | 2026-09 | 全套视觉重构；白日/黑夜双主题；退出确认卡片。 |
| 更早 | 2026-08 | 任务栏挂件条、双形态、Win11 兼容适配等。 |

---

## 五、常见问题（FAQ）

**Q1：任务栏挂件条不见了怎么办？**
A：右键日历空白处 →「设置」里重新打开挂件条，或把显示形态切回「插件」。

**Q2：日历点不出来 / 点别处不消失？**
A：挂件条只有小方框内响应鼠标，方框外是故意穿透的，请点方框本体。

**Q3：拖挂件条时松手就飞了 / 卡在半空？**
A：拖动受屏幕工作区限制，属正常；异常时重启软件即恢复默认位置。

**Q4：节假日数据准吗？**
A：节假日来自联网更新的官方安排，每年发布后自动更新，也可手动检查。

**Q5：怎么开机不自启？**
A：右键菜单 →「设置」里取消「开机自启」即可。

---

## 六、许可证

[MIT](LICENSE) © 2026 YG
