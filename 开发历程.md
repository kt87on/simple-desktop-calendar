# 开发历程（供版本更新参考）

> 本文档记录「简洁桌面日历」从 v1.0 到 v2.0 的开发过程、关键技术决策与踩坑复盘。
> **目的**：以后做版本更新 / 二次开发时，先读本文避免重蹈覆辙。
> 权威功能规格见 `PROGRAM_SPEC.md`，设计规范见 `DESIGN_SPEC.md` / `UI_DESIGN_SYSTEM.md`，版本变更摘要见 `CHANGELOG.md`。

---

## 1. 项目是什么

极简 Windows 桌面日历，核心只做三件事：**看日历、看放假、看上班**，外加「特别关注」到期提醒。形态 = 任务栏右端常驻实时时钟挂件条 + 点击唤出的无边框日历小窗。

**硬约束：不要擅自加功能**（无待办、无天气、无云同步、无账号）。

技术栈：Electron 31 + electron-builder 24（NSIS 单目标），纯 HTML/CSS/JS 零运行时依赖，农历用第三方 `lunar.min.js`（只读）。

---

## 2. 版本演进时间线

| 版本 | 里程碑 | 一句话 |
|---|---|---|
| v1.0.0 | 初始发布 | 基础日历 |
| v1.1.0 | 任务栏时钟替换 + 白/夜主题 | 挂件条雏形 |
| v1.2.0 | 稳定隐藏原生时钟 | 任务栏接管 |
| v1.4.0 | Node 原生 FFI（koffi）接管任务栏 | 消灭卡顿 |
| v1.7.11 | 任务栏挂件条 `dock.html` | 浮动方案奠基 |
| v1.7.13~19 | 7 次"真嵌入任务栏"尝试 | **全部失败（详见 §4）** |
| v1.7.20 | 彻底回退浮动方案 | 稳定基线 |
| v1.7.21 | 双形态 + 热区 + 拖动边界 | 交互定型 |
| v1.7.22~22.7 | 拖拽定位 bug 收尾 | `dockW/dockH` 唯一真值 |
| **v2.0.0** | **全套 UI 视觉重构** | 设计 Token 体系 + 图标 + 黑夜补齐 + 退出弹窗 |

---

## 3. 关键技术决策（为什么这么做）

### 3.1 放弃 Win32 任务栏时钟子类化，改用独立悬浮窗口（方案 B）
- 早期用 `SetWindowLong` 子类化 `TrayClockWClass` 覆盖原生时钟，问题多、脆弱。
- 最终形态：**置顶无边框透明窗口**贴任务栏上沿，视觉上"像长在任务栏里"，但完全不碰系统窗口。

### 3.2 透明无边框窗口 + `setIgnoreMouseEvents` click-through
- 挂件条窗口透明，只有 `#card` 矩形内响应鼠标，方框外彻底穿透。
- 实现：窗口常驻 `setIgnoreMouseEvents(true, { forward: true })`，页面按 `mousemove` 坐标判断是否在 `#card` 内，在内才收回响应。

### 3.3 固定像素布局（`pushDockSize` + `applyDockSize`）
- Chromium 在 transparent + 无边框 + 极小尺寸下，首帧可能按 ~16px viewport 渲染出"椭圆/被裁"。
- 主进程把物理窗口真实宽高发给页面，页面用固定 px 覆盖 `html/body/#card`，绕开 viewport 错算。
- **关键约束**：`padL=padT=2, padR=padB=0` 是"贴任务栏"的修复，绝不能回退。

### 3.4 拖拽绝对定位（禁止累加）
- mousedown 记 `offset = mousePos - windowPos`；mousemove 算 `newPos = mousePos - offset`。
- **禁止** `b.y += dy` 累加（累加会漂移）。

### 3.5 意图尺寸唯一真值（`dockW` / `dockH` + `dockClamped`）
- 物理窗口会被引擎/OS 撑大（请求 116×40，实测落到 232×164）。
- 所有定位/夹取一律用 `dockW/dockH` 意图尺寸，**不采信 `getBounds()` 的 width/height**。

### 3.6 v2.0 设计 Token 体系
- 5 色族 + 8 字阶 + 4pt 网格 + 5 圆角 + 5 elevation + 4 动效，全部在 L1 基础层 `:root` 集中声明。
- L2 语义层派生，L3 组件层只消费语义变量，**禁止硬编码色值/圆角/阴影**。
- `body.electron` 是 Electron 模式关键开关：`app.js` 加类，CSS 按此分流 browser/electron 布局策略。

---

## 4. 踩坑复盘（血泪教训，务必重读）

### 4.1 任务栏"真嵌入"是死路（v1.7.13~19）
- 做法：`SetWindowLong(GWL_STYLE, WS_CHILD) + SetParent(ReBarWindow32)`。
- 结果：**每次都被 Chromium 用 `render-process-gone reason=killed` 反噬**。
- 根因：SetParent 改父为 `WS_CHILD` 后，Chromium 渲染线程做 surface tree 自检失败 → 主动 kill 自己。不是 v1.7.18 以为的 setSize 问题。
- 衍生灾难：崩溃 → 重建 → 再嵌 → 又崩，进程堆积 + 单实例锁误杀新实例（表现为"打开闪崩"）。
- **教训：不要再碰任务栏嵌入。**

### 4.2 按进程名批量杀进程是错的（v1.7.20 首版）
- 清理残留时用 `tasklist` 按进程名全杀，误杀了 Electron 的渲染/GPU/utility 子进程（同名）。
- **教训：本程序清理残留必须按"父子关系"过滤，只杀不是自己子孙的同名进程。**

### 4.3 回退旧基线不能把"已踩的坑"一起回退
- v1.7.18 曾回退到旧基线，把已验证的坑又带回来（自愈条件写成恒真 `(wasEmbedded || true)` → 永动机）。
- **教训：回退要精确到"改了什么、为什么"，不能整包回滚。**

### 4.4 中文路径 / 编码坑
- `dock-attach.ps1` 必须有 UTF-8 BOM，否则 Windows 按 GBK 解码中文注释，字节吞掉换行符 → 语法错。
- 中文路径删目录偶尔失败 → 单文件 `rm -f` 仍可靠。

### 4.5 环境限制（开发/验证时注意）
- Bash 派生 GUI 进程被沙箱 373ms 回收 → 不能在 Bash 里跑 Electron 验证渲染，用无头 Edge 截图替代。
- `WScript.Shell` COM 被拦 → 桌面快捷方式必须用 `make-shortcut.js`（手写 MS-SHLLINK 二进制）。
- msedge `--window-size=W,H` 与 CSS viewport 不一致（实测 504×335 vs 请求 340×430）→ **精确视口截图必须用 CDP `Emulation.setDeviceMetricsOverride` + 原生 WebSocket（Node 22 自带）**。

### 4.6 校验脚本里的硬编码颜色断言
- v2.0 后今日色走 Token 链（`--blue-600 → --accent-strong`），旧断言检查 `#1976D2` 会误报。
- **教训：颜色断言要按 Token 链写 + `/i` 兼容大小写（实际是 `#1976d2` 小写）。**

---

## 5. 架构要点（改代码前必读）

### 5.1 文件职责
| 文件 | 职责 |
|---|---|
| `electron-main.js` | 主进程：窗口管理、托盘/菜单、拖拽 IPC、主题、退出、单实例锁、残留清理 |
| `preload.js` | contextBridge 桥接，暴露 `window.api.*`（IPC 收发） |
| `template.html` | 日历主体模板（`<style>` 内联全部设计 Token + 组件样式） |
| `app.js` | 日历渲染逻辑（注入到 calendar.html） |
| `dock.html` | 任务栏挂件条（时钟/日期 + 点击/拖动） |
| `reminder.html` | 特别关注提醒弹窗 |
| `remindlist.html` | 关注列表独立窗口 |
| `exit.html` | 退出确认弹窗（v2.0 新增） |
| `lunar.min.js` | 农历库（第三方，只读） |
| `holidays.js` | 节假日数据 |
| `build.js` | 生成 `calendar.html`（注入 lunar + app） |
| `make-icon.js` | 生成 `icon.ico` |
| `make-shortcut.js` | 生成桌面 .lnk（MS-SHLLINK 手写） |

### 5.2 关键不变量（绝不能破坏）
1. `padL=padT=2, padR=padB=0`（贴任务栏）。
2. 拖拽绝对定位 + 禁止累加。
3. `dockW/dockH` 意图尺寸唯一真值。
4. 设计 Token 只在 L1 声明，L3 不硬编码。
5. `body.electron` 布局开关。

---

## 6. 版本更新标准流程（照做即可）

```bash
# 1. 改代码 → 重新生成
node make-icon.js        # 重绘图标（如有改动）
node build.js            # 重建 calendar.html

# 2. 校验（4 套全绿才算通过，共 403 项）
node verify_v1721.js      # 源静态校验（143 项）
node smoke-test.js        # 冒烟测试（17 项）
node runtime-test_v1721.js # 运行时逻辑（103 项）
node verify_asar_v1721.js  # 打包产物反验（140 项）

# 3. 打包
npx electron-builder --win nsis   # 产物：dist/简洁桌面日历 Setup x.x.x.exe

# 4. 同步运行实例 + 桌面
#   dist/win-unpacked → D:\SimpleCalendar（按字节比较，未变跳过）
#   安装包 → 桌面
```

**部署落点**：
- 源码：`D:\AI\简洁日历`（正式版归档）
- 运行实例：`D:\SimpleCalendar\SimpleCalendar.exe`（用户实际双击的）
- 桌面快捷方式：`C:\Users\byab\Desktop\简洁桌面日历.lnk`
- 安装包：`C:\Users\byab\Desktop\简洁桌面日历 Setup x.x.x.exe`

---

## 7. 待办 / 已知残留

- `dist_prev_*` 等历史打包产物（约 2.9GB）按需手动清理，不自动删。
- 若日后做「跟随系统主题」：`electron-main.js` 已 import `nativeTheme`，可用 `nativeTheme.shouldUseDarkColors` 做默认值兜底（当前默认白天，用户已确认）。
