# 桌面日历 2.0 · 设计系统（UI Designer 方案）

> 在 1.0（功能确定版）基础上做**视觉 2.0 升级**。所有功能规则原样保留，本文件只定义「如何更美、更易用」。

## 1. 设计上下文（Design Context）
- **受众**：Win10 桌面常驻用户，需替代系统自带日历；含关注农历/节气/节假日的中国用户；含老花群体（放大模式）。
- **场景**：桌面角落常驻小窗；秒级看时间、看今天/农历/节假日；快速翻月；按个人习惯切换周一/周日起始。
- **调性**：清新、克制、有呼吸感、带东方日历的诗意；精致而非花哨。
- **约束**：固定竖长方形 widget（Electron 桌面应用）；浅色为主；Win10 兼容；保留 1.0 全部功能要素。

## 2. 美学方向：「纸与光」
一张浮在桌面上的半透明**宣纸卡片**，墨字写在纸上，自然光（灯效）扫过日期。
**避开 AI slop**：不用青+深底、不用紫蓝霓虹、克制毛玻璃（仅一层轻亚克力）、不用圆角+通用大阴影的俗套。

## 3. 设计 Token
### 色彩（暖纸白 + 墨色 + 雅蓝 + 朱砂）
| Token | 值 | 用途 |
|---|---|---|
| `--paper` | `rgba(252,251,249,0.82)` | 宣纸感底（非纯白） |
| `--ink` | `#1f2430` | 墨色主文字（非纯黑） |
| `--ink-soft` | `#6b7280` | 次级文字 |
| `--ink-faint` | `#9aa1ad` | 最弱/跨月文字 |
| `--accent` | `#3b6fd4` | 雅蓝/群青主色（替代 AI 标准蓝） |
| `--accent-soft` | `color-mix(accent 13%, transparent)` | 今日/选中淡底 |
| `--accent-line` | `color-mix(accent 52%, transparent)` | 今日/选中描边 |
| `--accent-ink` | `color-mix(accent 78%, #1f2430)` | 今日/选中文字（可读墨蓝） |
| `--cinnabar` | `#d6453f` | 朱砂红（节假日/休） |
| `--cinnabar-soft` | `rgba(214,69,63,0.07)` | 节假日淡底 |
| `--cinnabar-line` | `rgba(214,69,63,0.42)` | 节假日描边 |
| `--weekend` | `#c0564f` | 柔和砖红（周末主数字） |

> 跟随系统：仅覆盖 `--accent` 为 Win 强调色，其余全部由 `color-mix` 自动派生，整体配色一致联动。

### 排版
- 字体栈：`"Segoe UI Variable","Segoe UI","Microsoft YaHei","PingFang SC",system-ui`
- 时钟 `40px/600` 字距 1px（比 1.0 更舒展优雅）
- 日期 `13px/700`、农历 `11px/400`
- 表头 `9px/600` 字距 2px
- 单元格主数字 `16px/700`、副数字 `9px/500`

### 间距（8pt 节奏）
信息条 78px · 功能栏 42px · 网格 gap 3px · 单元格内 padding 4/3 · 外层圆角 16px · 单元格圆角 8px

### 深度
单层柔和阴影 `0 18px 44px rgba(31,35,48,.16)` + 内高光 `inset 0 1px 0 rgba(255,255,255,.65)`（不用多重投影堆叠）

## 4. 微交互
- 单元格 hover：轻微抬升 `translateY(-1px)` + 墨色细描边（沿用 1.0 清晰描边）
- **灯效**：淡雅蓝光晕跟随鼠标，中心亮、向外渐隐（沿用，颜色更柔）
- 今日/选中：雅蓝透明描边 + 极淡底（与 1.0 确认的"今日=选中"一致）
- **周起始开关**：真正的 toggle（轨道+滑块+两侧"一/日"标签），点击平滑过渡 cubic-bezier
- 月份切换：网格仅淡入（opacity），不缩放动画，避免 layout 抖动
- 按钮：hover 微变、active 轻微缩放

## 5. 布局（保留 1.0 顺序）
1. 信息条：时钟（左，大气）+ 日期/农历（右，精致右对齐）
2. 功能栏：年/月选择 + 箭头 + 今（左）；周起始 toggle + 主题 + 放大（右）
3. 日历网格：表头 + 6×7；月份水印背景；灯效层

## 6. 保留的 1.0 功能规则（不变）
周一/周日起始可切换 · 主数字公历+副数字农历/节气/节日白名单 · 节假日淡红+休 · 补班蓝+班 · 长假期仅节首日显示节日名 · 跨月透明红 · 节日白名单（24 节气+法定+中国传统+国外主流民俗，过滤国际/全国/行业类）· 今日=选中（浅蓝透明）· 灯效 · 主题默认/跟随系统 · 放大按钮（双击放大已移除）· 非本月更淡 · 消除点选闪烁
