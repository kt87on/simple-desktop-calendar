# 简洁桌面日历 · 设计系统（"纸与光"视觉语言）

> 本文档是视觉设计专项说明，配合 `PROGRAM_SPEC.md`（功能/行为规格书）使用。所有功能规则见 PROGRAM_SPEC，本文件只定义「如何更美、更易用」。

## 1. 设计上下文（Design Context）
- **受众**：Win10/11 桌面常驻用户，替代系统自带日历；含关注农历/节气/节假日的中国用户；含老花群体（放大模式）。
- **场景**：桌面角落常驻小窗；秒级看时间、看今天/农历/节假日；快速翻月；算两个日期之间天数；标记「特别关注」并到期提醒。
- **调性**：清新、克制、有呼吸感、带东方日历的诗意；精致而非花哨。
- **约束**：固定竖长方形 widget（Electron 桌面应用，340×430）；白日/黑夜双主题；Win10 兼容。

## 2. 美学方向：「纸与光」
一张浮在桌面上的半透明**宣纸卡片**，墨字写在纸上，自然光（灯效）扫过日期。
**避开 AI slop**：不用青+深底、不用紫蓝霓虹、克制毛玻璃（仅一层轻亚克力）、不用圆角+通用大阴影的俗套。

## 3. 设计 Token
### 3.1 色彩（暖纸白 + 墨色 + 雅蓝 + 朱砂；黑夜为墨纸白字）
| Token | 白日 | 黑夜 | 用途 |
|---|---|---|---|
| `--paper` | `rgba(252,251,249,0.82)` | `rgba(28,32,44,0.86)` | 宣纸/墨纸半透明底 |
| `--ink` | `#1f2430` | `#e8eaf0` | 墨色主文字 |
| `--ink-soft` | `#6b7280` | `#9aa3b3` | 次级文字 |
| `--ink-faint` | `#9aa1ad` | `#6a7283` | 最弱/跨月文字 |
| `--accent` | `#3b6fd4` | `#6b9bff` | 雅蓝/群青主色 |
| `--accent-soft` | `color-mix(accent 13%, transparent)` | `16%` | 今日/选中淡底 |
| `--accent-line` | `color-mix(accent 52%, transparent)` | `60%` | 今日/选中描边 |
| `--accent-ink` | `color-mix(accent 78%, #1f2430)` | `86%, #fff` | 今日/选中文字 |
| `--cinnabar` | `#d6453f` | `#ef6b65` | 朱砂红（节假日/休） |
| `--cinnabar-soft` | `rgba(214,69,63,0.07)` | `0.18` | 节假日淡底 |
| `--cinnabar-line` | `rgba(214,69,63,0.42)` | `0.55` | 节假日描边 |
| `--weekend` | `#c0564f` | — | 周末主数字（柔和砖红） |
| `--workday` | `#3b6fd4` | — | 补班/班 蓝 |
| `--holiday-fg` | `#cf463f` | — | 节假日文字 |

> 主题为**手动白日/黑夜切换**（非跟随系统强调色）。`--accent` 固定值，其余由 `color-mix` 自动派生。

### 3.2 状态色（硬编码，独立于 token）
| 状态 | 值 | 说明 |
|---|---|---|
| 区间端点 `.range` | 底 `rgba(34,197,94,0.16)` + 描边 `rgba(34,197,94,0.75)` | 深绿 + 绿描边 |
| 区间中间 `.range-between` | 底 `rgba(34,197,94,0.08)` | 浅绿无描边 |
| 区间数字 | `#15803d` | 端点与中间统一绿 |
| 特别关注 `.reminder` | 底 `rgba(255,193,7,0.22)` + 描边 `rgba(255,152,0,0.65)` | 金黄 + 橙描边 |
| 特别关注数字 | `#b26500`（黑夜 `#ffc857`） | 深金 |
| 关注角标 `.chip-focus` | `#ff9800` 底白字（黑夜 `#ffc107` 底墨字） | 左上角「注」（**v1.7.20 改名**，原「关」易被误读为"关闭"） |
| 月份水印 `#bgMonth` | 白日 `rgba(31,35,48,0.16)`；黑夜 `rgba(255,255,255,0.06)` | 无 text-shadow |
| 普通单元格 `.cell` | 白日 `transparent`；黑夜 `rgba(255,255,255,0.04)` | 透明化让水印透出 |

### 3.3 排版
- 字体栈：`"Segoe UI Variable","Segoe UI","Microsoft YaHei","PingFang SC",system-ui,sans-serif`
- 时钟 `30px/600` 字距 1px · 日期 `11px/700` · 农历 `10px/400`
- 表头 `8px/600` 字距 2px · 单元格主数字 `14px/700` · 副数字 `8px/500`
- 放大态（`#widget.max`）字号等比放大（约 2.24×，随窗口宽 760px 重画）

### 3.4 间距与尺寸（8pt 节奏）
- widget `340×430`（mini）→ 放大态 `760×959`（expanded，等比）
- 信息条 62px · 功能栏 38px · 网格 gap 2px · 单元格 padding 3/2
- 外层圆角 14px（放大 24px）· 单元格圆角 6px

### 3.5 深度
单层柔和阴影 `0 18px 44px rgba(31,35,48,.16)` + 内高光 `inset 0 1px 0 rgba(255,255,255,.65)`（不用多重投影堆叠）；黑夜改用深色阴影。

### 3.6 关注列表（独立窗口，**v1.7.12 改造**）
- 形态：**独立 `BrowserWindow`**（`remindlist.html`，340×440、无边框透明、可缩放、置顶），不再挂在 `#widget` 内——**可拖到屏幕任意位置**，不受日历窗口裁剪
- 卡片 `#card`：不透明 `#fdfcfa`（dark `#2b3040`），1px 描边 + 14px 圆角 + 双层阴影 `0 24px 60px rgba(31,35,48,.32), 0 2px 8px rgba(31,35,48,.12)`
- 标题栏 `#header`：`-webkit-app-region: drag`（系统原生拖动，`cursor:move`、`:active` 变 `grabbing`），左侧「📒 关注列表」+ `N/10` 计数胶囊，右侧 `✕` 关闭（`no-drag` 排除）
- **三列 grid**：`62px 1fr 24px`（日期列 / 内容列 / ✕ 列），内容列加宽、第三列列头**清空**（不显示「取消关注」四字，只留可点的 ✕ 位）
- 每行 hover：`rgba(31,35,48,0.035)` 极淡底；✕ 列 hover 浅红底 `rgba(220,38,38,0.12)` + 红字 `#dc2626`
- 内容列 `.rl-text-wrap`：`overflow-x:auto` + `cursor:grab`（按下 grabbing），按住左右拖看完整长文；隐藏滚动条
- 日期列点击：颜色切 `--accent`（雅蓝）+ 下划线作视觉反馈，同时跳转主窗该日

### 3.7 黄格 hover 浮层（v1.7.9 新增 / **v1.7.11 已移除**）
> **已删除。** 直接把鼠标指向格子的「注」角标即可看到内容，比等 2 秒更快。旧规范存档（**不要复刻**）：浮层 `#remindHoverTip`，绝对定位 z-index:60，深底 `rgba(31,35,48,0.92)` + 白字 + 8px 圆角 + 8px/22px 柔和阴影；默认放 cell 上方居中，上方放不下自动放下方；箭头通过 CSS 变量 `--tip-arrow-x` 定位到格子中心；字号 mini 11px/1.35、max 15px。

### 3.8 任务栏挂件条（v1.7.11 新增 / **v1.7.20：彻底废除嵌入、只保留浮动 + 去托盘 + 主窗右键兜底 + 拖动 + 去秒** / **v1.7.21：双形态 + 精确热区 + 工作区夹取 + 去吸附**）
- **形态（v1.7.21 起两种，二选一）**：116×（40~56，自适应任务栏高）无边框置顶窗口。
  - **桌面插件**（默认）：可拖动的小方框，**默认落点**在任务栏上沿、右端内缩 8px，视觉上等效"长在任务栏里"；用户拖到哪就在哪，位置持久化。
  - **桌面图标**：插件收起隐藏，只在系统托盘留一个静态图标；左键单击图标即切回插件并回到收起前的位置。
  - 两者互斥，由 `dockMode`（`'dock'` / `'icon'`）控制；托盘图标**只在图标形态下创建**（`createTray()` 首行 `if (dockMode !== 'icon') return;`）。
  > **【已废弃，不要复刻】v1.7.12~v1.7.19 的"真嵌入任务栏"**：`SetWindowLong(GWL_STYLE, …|WS_CHILD)` + `SetParent(ReBarWindow32)`。7 次尝试全部失败——Chromium 渲染线程在自身 HWND 被改父为 `WS_CHILD` 后会做 surface tree 自检，自检失败就主动 kill 自己（`render-process-gone reason=killed`），表现为每 30~50 秒崩一次、进程不断堆积。**根因是 SetParent，不是 v1.7.18 误判的 setSize。** v1.7.20 起 `dock-attach.ps1`、`attachDockToTaskbar()`、`dock-embedded` 通道等全部删除。
- 背景：`--card: rgba(248,249,251,.94)`，1px 描边 + 柔和阴影 + 圆角；hover 提亮、active 缩放 `scale(0.97)`。（旧版"嵌入成功后去边框去圆角"的 `body.embedded` 样式已随之废弃）
- 布局：上下两行居中——上行 24 小时制 `HH:MM`（**v1.7.20 起去掉秒**，等宽数字、较大字重），下行 `YYYY/MM/DD`（较小、`--ink-soft`）。
- 主题：随主窗 `theme-changed` 切换（dark 下反色）。
- 交互：左键开/隐藏主窗，右键弹功能菜单（**v1.7.20 起有两个入口**：挂件条右键，以及**主窗空白处右键**——后者是兜底，防止挂件条被关掉后无法恢复）；**按住可拖动**（>4px 判定为拖动，**v1.7.21 起松手不吸附**，改为拖动中与松手时各调一次 `clampDockToWorkArea()` 把插件夹进屏幕工作区并落库）；拖动 IPC 用 `requestAnimationFrame` 每帧最多一次，鼠标离开/失焦/`pointercancel` 主动复位（**v1.7.20**：杜绝"松手在外面、进了窗口继续拖"）；**不加任何 hover 提示浮层**（保持轻量感）
- **v1.7.20 回归修复（崩溃循环 / 进程堆积 / 显示不全）**：
  - **【已废弃】v1.7.18 的错误方向：`setSize` 强制重算 viewport**——v1.7.18 曾用「嵌入成功后 `setSize(W, H+1)` → 50ms 后 `setSize(W, H)`」试图修正 viewport 错算，**这反而放大了崩溃**。v1.7.20 已彻底移除所有 `setSize` 调用，并废弃嵌入方案本身。
  - **显示不全改用「固定像素布局」绕开 viewport**——Chromium 把 viewport 错算成 ~16px（物理窗口其实 40~56px）是既有行为，硬撑 resize 只会崩，所以改为绕开：主进程 `pushDockSize()` 把**物理窗口真实宽高**经 `dock-size` 通道下发给 `dock.html`，页面 `applyDockSize()` 用固定 px 覆盖 `html`/`body`/`#card`（统一内缩 2px，给边框阴影留位），彻底不依赖 `height:100%`。viewport 算错也照样按真实尺寸排布 → 内容完整，且完全不触碰窗口尺寸 → 不引发崩溃。
  - **崩溃循环终结**——v1.7.20 自愈条件为 `!quitting && dockOn && dockCrashed && dockRecreateCount < DOCK_RECREATE_MAX`（v1.7.18 曾误写成恒真的 `(wasEmbedded || true)`，导致任何关闭都重建，这是进程堆积的地基；v1.7.20 已改为只在真正崩溃时重建）。`DOCK_RECREATE_MAX=3` 硬上限，超限彻底放弃重建（窗口留白屏也好过无限堆进程）；`render-process-gone` 回调里**不同步 destroy**（改延迟到下一个 tick，避免 Electron 在崩溃上下文中二次踩雷）；稳定运行满 5 分钟（`dockStableTimer`）自动清零崩溃计数；手动从菜单重开挂件条时也会清零。
  - **残留进程清理（v1.7.20 内部修正，务必按此实现）**——启动时在 `whenReady` **最开头**（建任何窗口之前）调 `cleanupStaleInstances()`，配合 `requestSingleInstanceLock({ key: 'yg.simplecalendar.v1' })` 自定义命名空间，杜绝僵尸进程占锁导致"打开直接闪崩"。<br>**⚠️ 绝不要按进程名批量杀**：Electron 的渲染 / GPU / utility 子进程，可执行文件**同样叫 `SimpleCalendar.exe`**。首版用 `tasklist /FI "IMAGENAME eq SimpleCalendar.exe"` 列出后跳过自己 PID、其余全部 `taskkill`，结果 tasklist 异步返回（~400ms）时窗口已建好、子进程已 fork，被当成"残留"杀光 —— 挂件条刚建好就白屏、主进程随之退出。日志铁证：`[..43.571] dock created OK` → `[..43.972] cleanup: killing 4 stale peer(s)`。<br>**正确做法**：用 PowerShell `Get-CimInstance Win32_Process -Filter "Name='SimpleCalendar.exe'"` 拿 **PID + ParentProcessId**（tasklist 不给父 PID），以自己为根求子孙的**传递闭包**（`childrenOf` + BFS），只杀闭包之外的同名进程。残留旧实例的 PPID 是 `explorer.exe` 或已失效 PID，天然落在闭包外 → 精准命中。<br>**安全网**：取不到任何 PID/PPID 就记 `cleanup: no process info, skip` 并**放弃清理** —— 宁可留着旧进程，也绝不能误杀自己的渲染进程。
  - **移除 `unresponsive` 监听**——误报率过高（窗口加载/繁忙时都会触发），会把正常窗口误杀。
  - **吸附阈值**——`dock-drag-end` 距任务栏顶边 <80px 或已压到任务栏上方即吸附（v1.7.18 从 60 放宽）。原先"高度异常保护"已删除：`getBounds()` 取的是窗口**物理尺寸**，不受 viewport 影响，那段保护基于错误前提。

### 3.9 Toast 提示条（v1.7.11 新增）
- `#toast`：底部居中浮起，深色胶囊 + 白字（黑夜模式反色），12px 圆角
- 2.2s 自动淡出消失；同一时刻只显示一条（重复调用重置计时器）
- 用于「特别关注已满 10 条」等轻量提示，**不打断操作**（不用 `dialog.showMessageBox`）

### 3.10 皮肤风格与明暗轴（**v3.0.0**）
> v3.0.0 把皮肤从「单一 `type`」重构为 **`style`（风格材质）× `bg`（背景来源）** 两正交维度，并新增独立明暗轴 **`tone`**；文字轴 `text` 与整窗明暗**彻底解耦**（修复 v2 时代「一调深浅字、整窗变黑/白」的根因）。功能规则详见 PROGRAM_SPEC §4.6。

- **6 种风格（`style`）**：`default`（默认纸感）· `minimal`（极简）· `glass`（玻璃）· `neu`（新拟态）· `tech`（科技，自带深色）· `warm`（暖调）。风格决定材质与装饰层。
- **背景来源（`bg`）**：`native`（风格原生底）· `image`（自选图片，取景/清晰度见 PROGRAM_SPEC「图片皮肤」）。可与任意 `style` 叠加。
- **明暗轴（`tone`）**：`auto`（按风格自带：`tech`→深、其余→浅）· `light`（显式浅底）· `dark`（显式深底）· `system`（跟随系统深浅）。整窗 `data-theme` 由此推导。
- **文字轴（`text`）**：`auto|light|dark`，**仅**控制文字深浅，不再联动整窗明暗。
- **低对比提示（`warn`）**：浅底配浅字 / 深底配深字时，皮肤窗给出提示，且渲染层把清晰度下限抬到 35，保证可读。
- **图片优先**：`bg=image` 且有图时，整窗明暗按照片亮暗（`image.dark`，压过 `tone`）。

## 4. 微交互
- 单元格 hover：轻微抬升 `translateY(-1px)` + 墨色细描边
- **灯效**：淡蓝径向光晕跟随鼠标，中心亮、向外渐隐
- 今日/选中：雅蓝透明描边 + 极淡底（与"选中"同效果）
- **周起始开关**：真正的 toggle（轨道+滑块+两侧"一/日"标签），点击平滑过渡 cubic-bezier
- 月份切换：网格仅淡入（opacity），不缩放动画，避免 layout 抖动
- 按钮：hover 微变、active 轻微缩放

## 5. 布局（顺序固定）
1. `#infoBar`：时钟（左，大气）+ 日期/农历（右，精致右对齐）
2. `#dragBar`：年/月选择 + 箭头 + 今（左）；周起始 toggle + 主题 + 放大（右）
3. `#calendar`：表头 + 6×7 网格；`#bgMonth` 月份水印 + `#litGlow` 灯效层

## 6. 功能规则速览（详见 PROGRAM_SPEC.md）
周一/周日起始可切换 · 公历主数字 + 农历/节气/节日白名单 · 节假日淡红+休 · 补班蓝+班 · 长假期仅节首日显示节日名 · 跨月淡化 · 今日=选中（雅蓝描边）· 灯效 · 白日/黑夜主题 · 放大按钮（**v1.7.20** 改成左上「大」/ 右下「小」两字**一样大** + 中间斜杠 图标，等比缩放）· 区间选择（端点深绿/中间浅绿）· 特别关注（金黄+注角标）· 月份水印
